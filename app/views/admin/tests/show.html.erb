<h1>Test "<%= @test.title %>"</h1>

<%= link_to 'New question', new_admin_test_question_path(@test) %>

<h2>Total <%= pluralize(@test.questions.count, 'question') %>:</h2>

<%= render @test.questions %>

<p><%= link_to 'Back', admin_tests_path %></p>
